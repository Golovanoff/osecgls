#Использовать "../.."

&ЖурналПроверки
Перем Лог;

&ПроверкаМР
Процедура ПриСозданииОбъекта()

КонецПроцедуры

Функция ВыполнитьПроверку(Знач ТелоЗапроса) Экспорт
	
	Лог.Отладка("Проверка статуса заявки Jira.");

	Проект = "IMPLIC";

	Если СтрНайти(ТелоЗапроса["title"], Проект) > 0 Тогда
		Возврат Общие.РезультатПроверки(Истина);
	Иначе
		Возврат Общие.РезультатПроверки(Ложь, "Всё плохо!");
	КонецЕсли;
КонецФункции
